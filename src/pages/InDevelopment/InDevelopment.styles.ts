import styled from "styled-components";

import * as C from "styles/components";

export const InDevContainer = styled(C.Container)`
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 10px;

    height: 100%;
`;
